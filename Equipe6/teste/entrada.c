#include <stdio.h>
#define MAX 10
#include "lista.h"
int main(int argc, char *argv[]){
        int i,num;
        char str[40];

        for(i=0;i<10;i++)
        {
            scanf("%d",&num)
            scanf("%s",str);
            Item tempo = insere_item(num,str);
            insert
        }
        for(i=0;i<MAX;i++)
        {
            printf("%d\n",str[i]);
        }
        return 0;
    }
